{
  "name": "@postio/packages",
  "private": true,
  "description": "Postio JS-family monorepo: drop-in JS, npm core, React, MCP server, CLI, scaffolder, OpenAPI tooling. See ../SPEC.md.",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r --filter './*' run build",
    "lint": "pnpm -r --filter './*' run lint",
    "test": "pnpm -r --filter './*' run test"
  }
}
